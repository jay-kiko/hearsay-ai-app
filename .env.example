# The backend now owns the Anthropic key and admin auth (a single shared
# secret checked server-side) — nothing sensitive needs to live here anymore.
# Just where the backend is running.
VITE_API_BASE_URL=http://localhost:8001
