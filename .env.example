# Frontend-only stand-in for real auth — the backend replaces this with actual
# admin accounts. Anything in a Vite env var still ships in the client bundle,
# so this keeps credentials out of source, not out of the browser.
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=kiko-staff-2026
